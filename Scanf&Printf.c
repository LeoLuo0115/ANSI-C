#include <stdio.h>
// cretate a function that plus a,b
int SolveEquation(int a, int b)

{
return a+b;
}
int main ()
{
int num1, num2;
scanf("%d %d", &num1, &num2);
int sum
sum = SolveEquation(num1,num2);
printf("%d",sum);
return 0
}